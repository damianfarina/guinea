<% content_for :breadcrumbs do %>
  <h3 class="top-bar__title"><%= t(:edit_resource, scope: [:common], resource: Veterinary.model_name.human) %></h3>
<% end %>

<%= render 'form', veterinary: @veterinary %>

<%= link_to 'Show', @veterinary %> |
<%= link_to 'Back', veterinaries_path %>
