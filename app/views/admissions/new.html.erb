<% content_for :breadcrumbs do %>
  <h3 class="top-bar__title"><%= t(:new_resource, scope: [:common], resource: Admission.model_name.human) %></h3>
<% end %>

<%= render 'form', admission: @admission %>

<%= link_to 'Back', admissions_path %>
