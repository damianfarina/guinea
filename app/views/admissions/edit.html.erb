<% content_for :breadcrumbs do %>
  <%= link_to Admission.model_name.human(count: 2), admissions_path, { class: 'breadcrumbs__item' } %>
  <span class="breadcrumbs__separator"></span>
  <%= link_to @admission.id, @admission, { class: 'breadcrumbs__item' } %>
  <span class="breadcrumbs__separator"></span>
  <h3 class="breadcrumbs__item breadcrumbs__item--active"><%= t(:edit_resource, scope: [:common], resource: Admission.model_name.human) %></h3>
<% end %>

<%= render 'form', admission: @admission %>
