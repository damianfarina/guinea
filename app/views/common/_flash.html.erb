<div class="flash__container">
  <% flash.each do |key, value| %>
    <div class="flash flash--<%= key %>">
      <%= value %>
      <button class="flash__close" onclick="this.closest('.flash').remove();return false;">&times;</button>
    </div>
  <% end %>
</div>
